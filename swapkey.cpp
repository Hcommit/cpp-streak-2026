#include<iostream>
using namespace std;
int main(){
    int a=5;
    int b=6;

    swap(a,b);   // it is used to exchange values for in this case a
                 // a will become 6 and b will become 5
    cout<<a<<endl<<b;
}